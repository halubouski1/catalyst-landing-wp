<?php
/**
 * Template Name: Landing RU
 *
 * Russian version of the landing. Assign this template to a Page with the
 * slug "ru" (so it lives at /ru/). The language is switched to Russian on
 * template_redirect (see catalyst_lang in functions.php).
 *
 * @package Catalyst
 */

get_header();

get_template_part( 'template-parts/content-landing' );
get_footer();
